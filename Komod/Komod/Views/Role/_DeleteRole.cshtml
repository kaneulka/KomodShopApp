@model RoleViewModel
@using Komod.Web.Models.UserModels
@using Komod.Web.Models

<form asp-action="DeleteRole" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Удаление роли" })

    <div class="modal-body">
        <input asp-for="Id" />
        Вы уверены, что хотите удалить @Model.Name?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Удалить" })
</form>