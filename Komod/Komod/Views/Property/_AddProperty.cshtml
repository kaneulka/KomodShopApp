@model PropertyViewModel
@using Komod.Web.Models.PropertyModels
@using Komod.Web.Models

<form asp-action="AddProperty" role="form">
    @await Html.PartialAsync("_ModalHeader", new ModalHeader { Heading = "Добавление характеристику" })
    <div class="modal-body">
        <div class="row">
            <div class="col-12">
                <div class="form-group">
                    <span asp-validation-for="Name"></span><br />
                    <label asp-for="Name"></label>
                    <input asp-for="Name" class="form-control" />
                </div>
                <div class="form-group">
                    <label asp-for="ValueName"></label>
                    <input asp-for="ValueName" class="form-control" />
                </div>@*
        <div class="d-flex flex-wrap">
            @foreach (var category in Model.AllCategories)
            {
                <div class="form-group form-check ml-3">
                    <label class="form-check-label">
                        <input class="form-check-input" type="checkbox" name="categories" value="@category.Name" /> @category.Name
                    </label>
                </div>
            }
        </div>*@
            </div>
        </div>
    </div>
    @await Html.PartialAsync("_ModalFooter", new ModalFooter { })
</form>


